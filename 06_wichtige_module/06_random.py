"""
Das random-Modul in Python ermöglicht das Erzeugen von Zufallszahlen
und das zufällige Auswählen von Elementen aus Sequenzen. Es eignet
sich für Simulationen, Spiele oder Zufallsexperimente.

Wichtige Funktionen:
- random.random(): Zufallszahl zwischen 0.0 und 1.0
- random.randint(a, b): Ganzzahl zwischen a und b (inklusive)
- random.choice(seq): Zufälliges Element aus einer Sequenz
- random.shuffle(seq): Durchmischt eine Sequenz in-place
- random.uniform(a, b): Fließkommazahl zwischen a und b
- random.gauss(mu, sigma): Zufallswert nach der Gaußschen Verteilung
- random.choices(seq, k=n): Ziehe n Elemente mit Zurücklegen
- random.sample(seq, k): Ziehe k eindeutige Elemente ohne Zurücklegen
"""

# Importiere das random-Modul zur Erzeugung von Zufallswerten
import random

# Erzeuge eine Zufallszahl zwischen 0.0 und 1.0

# Wähle zufällig ein Element aus einer Liste

# Mische eine Liste zufällig durch

# Erzeuge eine Zufalls-Fließkommazahl zwischen 5.0 und 10.0

# Aufgabe: Erzeuge eine Zufalls-Fließkommazahl zwischen -1.0 und 1.0

# Erzeuge eine Zufallszahl nach der Gaußschen Normalverteilung
# Format: random.gauss(mu, sigma) -> Mittelwert mu, Standardabweichung sigma

# Aufgabe: Erzeuge eine Gauß-verteilte Zufallszahl mit Mittelwert 5 und Standardabweichung 2

# Ziehe mehrere Elemente mit Zurücklegen aus einer Liste


# Ziehe mehrere eindeutige Elemente ohne Zurücklegen aus einer Liste
